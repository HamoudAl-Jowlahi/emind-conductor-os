# مهام eMind Conductor OS

> ملف العمل. قل **«ابدأ بالمهمة ٣»** وسأنفّذها كاملة: كود + اختبارات + توثيق + دفع.
> آخر تحديث: 2026-08-20

**الحالة:** ⬜ لم تبدأ · 🟨 جارية · ✅ منجزة

---

## الهدف النهائي

نظام مستضاف على الإنترنت، يسجّل فيه أي شخص، **يضع مفاتيحه هو**، وتعمل وكلاؤه على **بياناته هو** بمعزل تام عن غيره.

## القرار الحاكم — لا يُخالَف

> **كل جدول جديد يولد ومعه `user_id`.**

السبب: بناء ميزة بمنطق «مستخدم واحد» ثم إضافة العزل لاحقاً = تنفيذ العمل مرتين (ترحيل + مستودع + واجهة + اختبارات). العمود يكلّف دقائق الآن، ويكلّف أياماً لاحقاً.

## شرط النشر — غير قابل للتفاوض

**لا تسجيل عام على الإنترنت قبل اكتمال المهمة ٥.** حتى ذلك الحين: دعوات فقط، أو حماية على مستوى الخادم.

## قرار تصميم مثبّت (2026-08-20)

الإعدادات للحساب لا للعمل:

| المكان | المحتوى |
|---|---|
| `/integrations` | الاتصالات + **إدخال المفاتيح** |
| `/agents` | الطاقم + **إنشاء وكيل** |
| `/settings` | الحساب: الاسم · البريد · كلمة المرور · الجلسات |
| الشريط العلوي | **الثيم يبقى هنا** |

---

# المهام

## ✅ ١ — كتالوج الوكلاء + طبقة التثبيت *(2026-08-20)*

**الهدف:** يستعرض المستخدم الوكلاء المتاحين، يقرأ ما يفعل كل واحد ومتطلباته، ويثبّت ما يلزمه. طاقمه = ما اختاره، لا الـ١٩ كلهم.

**لماذا هذه أولاً:** جدول `user_agents` **مطلوب على أي حال** في نظام متعدد المستخدمين — يجب أن تعرف أي وكلاء يملك كل مستخدم. المتجر يعطي هذا الجدول الإلزامي معنىً، ويجعل المهمة ١ب ترث كل شيء منه.

**العمل**
- جدول `user_agents` **بـ `user_id` من الولادة**: `user_id · agent_id · source ('builtin'|'custom') · enabled · config`
- الـ١٩ وكيلاً الحاليون = الكتالوج. **لا نكتب وكيلاً جديداً**
- صفحة الكتالوج: البطاقة تعرض العمل والقسم و**المفاتيح المطلوبة** (من حقل `tools` الموجود أصلاً)
- تثبيت / إلغاء / تعطيل
- `/agents` يعرض **المثبَّت فقط**

**القبول**
- «أ» يثبّت ٣ وكلاء، «ب» يثبّت وكيلين مختلفين — لا تداخل
- البطاقة تقول بصراحة «يحتاج `SLACK_BOT_TOKEN`» قبل التثبيت
- المجدول يشغّل الوكلاء المثبَّتين فقط
- **الوكيل المثبَّت يظهر في كل مكان يظهر فيه الطاقم**: `/agents` · هيكل `/org` · رسم `/brain` · المجدول. الطاقم مصدر واحد، لا أربعة
- **بثّ Conductor ينحصر في المثبَّت.** البثّ إلى وكيل غير مثبَّت يعني محاولة استخدام مفتاح لا يملكه المستخدم — فشل بلا معنى في أحسن الأحوال

**قرار مثبّت:** إضافة وكيل جديد للكتالوج تتم **بالكود** (`real.ts` + `seed.ts` + اختبار + push)، **لا بلوحة إدارة**. قيمة الوكيل هي كوده؛ ولوحة تأليف لن تنتج إلا وكيل تعليمات نصية — وهو ما تفعله المهمة ١ب أصلاً. وبالكود تحصل على مراجعة واختبارات وتراجع.

**تسمية دقيقة:** هذا **كتالوج** لا متجر. إضافات VS Code يكتبها طرف ثالث؛ هنا نكتبها نحن. فتحه لمؤلفين خارجيين مشروع منفصل (عزل · مراجعة · ثقة) — **ليس امتداداً بسيطاً**.

**الجهد:** ٣ أيام · **يعتمد على:** لا شيء

---

## ⏸️ ١ب — الوكلاء الديناميكيون *(مؤجَّلة بقرارك — مرشّحة لتكون ميزة مدفوعة)*

**الهدف:** ينشئ المستخدم وكيلاً من نموذج في `/agents`: اسم + وصف عمله + جدول. يركب على نفس جدول المهمة ١ بـ `source: 'custom'`.

**العمل**
- نموذج الإنشاء + `POST /api/agents/custom`
- **النموذج يسأل عن الركيزة** (مبيعات · تسويق · تقنية · مالية · اتصالات · عملاء) — وإلا طفا الوكيل في `/org` بلا انتماء
- `lib/agents/custom.ts` — الوصف يصير `system prompt`، ويشغّله Gemini
- تعديل اختبار التطابق 1:1 ليقبل الوكلاء المخصّصين

**القبول**
- أنشئ وكيلاً → يظهر في الطاقم → شغّله → رد حقيقي
- **يظهر تحت ركيزته في `/org` وكعقدة في `/brain`** — تماماً كالوكلاء المدمجين
- جدوله `*/5 * * * *` → يعمل وحده
- وكيل «أ» لا يظهر لـ«ب»

**صريح:** الوكيل المخصّص **يقرأ ويحلل ويبلّغ. لا ينفّذ أفعالاً خارجية** — ذلك في المهمة ٩. الوصف النصي لا يمنح أدوات؛ الأدوات تُكتب بالكود.

**مؤجَّلة (2026-08-20).** القرار: تصلح كميزة في الباقة المدفوعة لا في المجانية — وهو تمييز منطقي، لأن كل وكيل مخصّص يستهلك استدعاءات نموذج لغوي، فالتكلفة تنمو مع الاستخدام. تنفيذها يحتاج المهمة ١٠ (تتبّع التكلفة) لتسعيرها بإنصاف.

**الجهد:** يوم واحد (يرث طبقة التثبيت) · **يعتمد على:** ١ · للتسعير: ١٠

---

## ✅ ٢ — عزل البيانات: `user_id` على الجداول الـ٣٠ *(2026-08-20)*

**الهدف:** أساس تعدد المستخدمين. بدونه يرى كل مستخدم بيانات الجميع.

**العمل**
- ترحيل يضيف `user_id` لكل جدول يحمل بيانات مستخدم (الوكلاء، التشغيلات، الرسائل، القمع، المالية، الاجتماعي، المهام…)
- **فرض العزل في طبقة المستودعات** — لا في الصفحات. `getDb()` يصير مربوطاً بالمستخدم الحالي
- كل صفوف اليوم تُنسب لأول مستخدم (المالك)
- اختبار يفرض: **لا استعلام يعبر حدود المستخدم**

**القبول**
- مستخدمان، بيانات مختلفة تماماً، صفر تسريب
- اختبار يفشل عمداً لو نسي مطوّر شرط `user_id`

### ما أُنجز

| البند | التفصيل |
|---|---|
| التصنيف | `lib/tenancy.ts` — ٧ مشتركة · ٤ هوية · **٢٠ مملوكة**، واختبار يسقط لو أُضيف جدول بلا تصنيف |
| الترحيل | `user_id` **+ فهرس** على الجداول العشرين |
| التقييد | **٥٨ عبارة مُعدّة** تحمل الآن شرط المالك |
| المقبض المقيَّد | `openDb` صار اتصالاً + مصنع مستودعات · `db.withUser(id)` · `currentDb()` |
| الربط | ١٣ صفحة + **١٢ مسار برمجي** + المجدول |
| مسار الترقية | `claimOrphanRows()` — أول مستخدم يرث التثبيت، والتالي يبدأ فارغاً |
| الحارس الساكن | يقرأ `lib/db.ts` ويسقط لو مسّ استعلامٌ جدولاً مملوكاً بلا ذكر المالك |

**قاعدة الربط الموحّدة:** شرط النطاق **آخر** ما في `WHERE`، فربطه **آخر** معامل، و`LIMIT` يُضمَّن كعدد صحيح لا كعلامة استفهام — لأن `better-sqlite3` يرفض مزج المعاملات المسمّاة بالموضعية، و`LIMIT ?` بعد النطاق كان سيفرض المزج.

**تحقّق حيّ:** مالك وضيف على خادم واحد — المالك `activity=1 funnel=13 metrics=4`، الضيف **`0 · 0 · 0`**.

**الجهد:** ١٠–١٤ يوماً · **يعتمد على:** ١ · **أثقل مهمة في الملف**

---

## ✅ ٣ — مخزن مفاتيح مشفّر لكل مستخدم *(2026-08-20)*

**الهدف:** يضع كل مستخدم مفاتيحه، مشفّرة، معزولة. **يحلّ محل `.env.local` المشترك** الذي يمسح فيه مفتاح مستخدم مفتاح الآخر.

**العمل**
- جدول `user_credentials` (`user_id`, `key_name`, `ciphertext`, `iv`)
- تشفير AES-256-GCM بمفتاح جذر من البيئة (`node:crypto`، بلا حزم جديدة)
- إعادة كتابة `lib/creds.ts`: يقرأ اليوم من ملف عند الإقلاع → يقرأ مفاتيح **المستخدم الحالي** في كل طلب
- `/integrations` يكتب إلى المخزن بدل الملف — **الواجهة موجودة، المخزن فقط يتغير**

**القبول**
- «أ» و«ب» لهما مفتاح Stripe مختلف، وكل وكيل يستخدم مفتاح صاحبه
- لا مفتاح خام في قاعدة البيانات (تحقق مباشر بالاستعلام)
- لا مفتاح يُعاد إلى الواجهة أبداً — `set` / `not set` فقط

### ما أُنجز

| البند | التفصيل |
|---|---|
| التشفير | AES-256-GCM من `node:crypto` — **بلا حزمة جديدة**. GCM موثَّق، فالصف المعبوث يفشل بصوت عالٍ لا يفكّ إلى قمامة تُرسَل لواجهة حيّة |
| المفتاح الجذر | `CREDENTIALS_KEY` في البيئة، **لا في القاعدة** — من يسرق ملف القاعدة يحصل على تشفير فقط. وبدونه يرفض المخزن الحفظ بدل الحفظ الصريح |
| الجدول | `user_credentials` — `(user_id, key_name)` مفتاحاً أساسياً، ولا دالة «أعطني كل المفاتيح» |
| وصول الموصلات | `AsyncLocalStorage` في `lib/creds.ts` — الـ١٢ ملفاً التي تستدعي `resolveCred`/`runtimeEnv` استفادت **بلا تعديل واحد** |
| الربط | لوحة الاتصالات · تشغيل الوكيل · المحادثة · البثّ · المجدول |
| الواجهة | `/integrations` يكتب إلى المخزن بدل الملف — **الواجهة لم تتغير** |

**تحقّق حيّ (نفس الخادم، نفس اللحظة):**
- كلٌّ حفظ مفتاح Notion مختلفاً → **لا مفتاح خام في القاعدة** (فحص مباشر بـ sqlite)
- بعد حذف الضيف لمفتاحه: المالك `error · Key set but auth failed` · الضيف `not_configured`
- حذف الضيف لم يمسّ صفّ المالك

**الجهد:** ٣ أيام · **يعتمد على:** ٢

---

## ✅ ٤ — صفحة `/settings` للحساب *(2026-08-20)*

**الهدف:** ما ليس له بيت آخر. **لا تنقل الثيم ولا التكاملات ولا الوكلاء إليها.**

**العمل**
- الاسم · البريد · تغيير كلمة المرور
- الجلسات النشطة + «سجّل الخروج من كل الأجهزة»
- حذف الحساب

### ما أُنجز

| البند | التفصيل |
|---|---|
| الملف الشخصي | الاسم والبريد · البريد يُصغَّر تلقائياً · بريد مستخدَم من حساب آخر يُرفض، وبريدك أنت ليس تعارضاً |
| كلمة المرور | تتطلب الحالية · **وتُخرج كل الأجهزة الأخرى** وتُبقي التي غيّرت |
| الأجهزة | عدد الجلسات + «سجّل الخروج من كل الأجهزة» — **بلا عرض الرموز إطلاقاً** |
| حذف الحساب | مؤكَّد بكلمة المرور · يمسح البيانات والمفاتيح والطاقم والجلسات في معاملة واحدة |

**قراران:**
- **الجلسات تُعرض بلا معرّفاتها** — المعرّف هو الرمز الحامل نفسه، وعرضه في قائمة يسلّم الجلسة لمن يرى الشاشة.
- **تغيير كلمة المرور يُنهي بقية الجلسات.** السبب المعتاد للتغيير أن أحداً آخر يملكها، فإبقاء جلسته يُبطل الغرض.

**عقبة تقنية:** جداول المستخدم يشير بعضها إلى بعض (`funnel_touches → funnel_contacts`)، فأي ترتيب حذف ثابت خاطئ لزوج ما وسينحرف مع كل جدول يُضاف. الحل: `defer_foreign_keys` داخل المعاملة — يسمح بأي ترتيب ويرفض الالتزام برسم مكسور.

**تحقّق حيّ:** كلمة مرور خاطئة `400` · الجهاز المغيِّر بقي `200` والآخر خرج `307` · القديمة ماتت `401` · الحذف نقل كل شيء من `1/1/4/19` إلى `0/0/0/0`.

**الجهد:** يوم · **يعتمد على:** ٢

---

## ⬜ ٤ب — الدخول بحساب Google

**الهدف:** «سجّل الدخول بـ Google» بجانب البريد وكلمة المرور. مدخلان، وجلسة واحدة.

**لماذا قبل النشر:** يقلّص المهمة ٥ لا يضيف إليها — لا كلمة مرور تُنسى أو تُسرَّب، والبريد يصل **موثّقاً**، و**Google يتولى التحقق بخطوتين** نيابةً عنك.

**العمل**
- `GET /api/auth/google` → إعادة توجيه مع `state` موقّع (حماية CSRF)
- `GET /api/auth/google/callback` → تبادل الرمز → قراءة الاسم والبريد → إيجاد المستخدم أو إنشاؤه → **`createSession` نفسها**
- عمود `google_sub` على `users` للربط الثابت
- زر في `LoginForm`

**بلا حزمة جديدة.** نحتاج **الهوية فقط لا الوصول إلى خدمات Google** — فلا تجديد رموز ولا حفظها ولا PKCE. وAuth.js كان سيطالب بامتلاك طبقة الجلسات المبنية والمختبرة، فنستبدل ما يعمل بما هو أثقل، مقابل مزايا لا نحتاجها.

**قرار الربط:** بريد واحد = حساب واحد بمدخلين. **الشرط الأمني:** الربط فقط إن أعاد Google `email_verified: true` — وإلا استطاع مهاجم أن يسجّل ببريد الضحية قبلها فيرث حسابه دخولها.

**القبول**
- دخول بـ Google لمستخدم جديد → حساب + جلسة
- نفس البريد الذي سجّل بكلمة مرور → **نفس الحساب**، لا ثانٍ
- بريد غير موثّق من Google → رفض الربط
- `state` مفقود أو غير مطابق → رفض الطلب

**يحتاج منك:** مشروع Google Cloud ← شاشة موافقة ← `OAuth 2.0 Client ID` (Web application) ← `GOOGLE_CLIENT_ID` و`GOOGLE_CLIENT_SECRET`. رابط الإرجاع: `/api/auth/google/callback` (نسخة للتطوير وأخرى للنطاق الحقيقي).

**الجهد:** يوم · **يعتمد على:** ٢

---

## ⬜ ٥ — التقوية الأمنية (شرط النشر)

**الهدف:** ما أجّلناه. **لا نشر عام قبل إتمامها.**

**العمل**
- حد لمحاولات الدخول (rate limiting)
- اشتراط تعقيد كلمة المرور — اليوم ١٠ محارف فقط بلا شروط
- استرجاع كلمة المرور بالبريد *(لمن سجّل بكلمة مرور فقط — مستخدمو Google لا يحتاجونه)*
- تحقق بخطوتين (TOTP) *(نفس الملاحظة — Google يتولاه لمستخدميه)*
- سجلّ دخول: من · متى · من أين
- فتح التسجيل العام (كان محصوراً بأول مستخدم)

**الجهد:** ٤ أيام · **يعتمد على:** ٢

---

## ⬜ ٦ — تجهيز النشر

**العمل**
- Docker + docker-compose — يحل مشكلة `better-sqlite3` نهائياً
- **الهجرة من SQLite إلى Postgres** (كاتب واحد لا يكفي لعدة مستخدمين)
- GitHub Actions CI
- معالجة الثغرات الـ13 يدوياً — **لا `npm audit fix --force`**
- نسخ احتياطي

**الجهد:** ٥ أيام · **يعتمد على:** ٢ · ٥

---

## ⬜ ٧ — النشر

الاستضافة · النطاق · HTTPS · المراقبة · فتح التسجيل.

**الجهد:** يومان · **يعتمد على:** ٦

---

# بعد النشر

## ⬜ ٨ — طابور مهام غير متزامن
التشغيل اليوم داخل طلب HTTP؛ أي مهمة طويلة تموت. **الجهد:** أسبوع

## ⬜ ٩ — وكلاء بصلاحية كتابة + موافقة بنقرة
الوكيل يقترح، أنت تعتمد. **هذا ما يجعل الوكيل «يفعل» فعلاً.** **الجهد:** أسبوع

## ⬜ ١٠ — تتبّع تكلفة الاستدعاءات
كم أنفق كل مستخدم؟ يلزم قبل أي اشتراك. **الجهد:** يومان

## ⬜ ١١ — بحث دلالي حقيقي لـ G-Brain
البحث الآن `grep` عند فشل القاعدة. **الجهد:** ٣ أيام

## ⬜ ١٢ — تحديث لحظي (SSE) · اختبارات E2E بـ Playwright
**الجهد:** ٤ أيام

## ⬜ ١٣ — لوحة قياس للمالك (Admin)

**الهدف:** **قياس لا إنشاء.** لوحة تراها أنت وحدك: من يستخدم النظام، وأي وكيل ينجح وأيّها يفشل.

**لماذا بعد النشر:** لا معنى لقياس بلا مستخدمين. وقبل ذلك الأرقام صفر.

**ما تعرضه**

| المحور | المقياس |
|---|---|
| النمو | تسجيلات جديدة · مستخدمون نشطون (٧ و٣٠ يوماً) · معدل البقاء |
| الوكلاء | **كم مستخدماً ثبّت كل وكيل** · ترتيب الأكثر تثبيتاً · الأكثر إلغاءً |
| الجودة | **نسبة نجاح تشغيلات كل وكيل** · الأكثر فشلاً · أسباب الفشل المتكررة |
| الاتصالات | أي مفاتيح يوصّلها الناس فعلاً · أيّها يتجاهلونه |
| التكلفة | استهلاك النموذج اللغوي لكل مستخدم (يعتمد على المهمة ١٠) |

**الخبر الجيد:** أهم البيانات **موجودة أصلاً**. جدول `agent_runs` يسجّل كل تشغيل بحقل `ok` وملخّص السبب منذ اليوم الأول — نسبة النجاح وأسباب الفشل استعلام واحد، لا بناء جديد.

**العمل**
- حقل `role` على المستخدم (`owner` / `user`) + حارس يمنع غير المالك — **لا توجد أدوار اليوم**
- `/admin` محمية بذلك الحارس
- استعلامات تجميع + رسوم بلغة المشروع البصرية
- **بلا بيانات شخصية**: أرقام مجمّعة، لا محتوى رسائل ولا مفاتيح ولا بيانات عملاء المستخدمين

**القبول**
- مستخدم عادي يفتح `/admin` → ٤٠٣ لا صفحة فارغة
- نسبة نجاح كل وكيل تطابق `agent_runs` يدوياً
- لا حقل يعرض محتوى بيانات أي مستخدم

**الجهد:** ٣ أيام · **يعتمد على:** ٧ (النشر) · ١٠ لعمود التكلفة

---

# ✅ المنجز

| | العمل | التاريخ |
|---|---|---|
| ✅ | ترخيص MIT + إسناد المؤلف الأصلي | 2026-08-20 |
| ✅ | ترقية Next 14 → 16 وإصلاح المسارات الديناميكية | 2026-08-16 |
| ✅ | إعادة التسمية + الشعار + ثيم eMind | 2026-08-16 |
| ✅ | تطهير بيانات المالك السابق (`lib/operator.ts`) | 2026-08-20 |
| ✅ | مزوّد Gemini — محادثة الوكلاء تعمل بلا بطاقة | 2026-08-20 |
| ✅ | المصادقة: جلسات · صفحة دخول · مجموعة محمية | 2026-08-20 |
| ✅ | مُشغّل الجدولة — الوكلاء يعملون تلقائياً | 2026-08-20 |
| ✅ | حذف ١١ وكيلاً (٣٠ → ١٩) | 2026-08-20 |
| ✅ | الرفع إلى GitHub | 2026-08-20 |
| ✅ | **المهمة ١ — كتالوج الوكلاء + طبقة التثبيت** | 2026-08-20 |
| ✅ | **المهمة ٢ — عزل بيانات المستخدمين** | 2026-08-20 |
| ✅ | **المهمة ٣ — مخزن مفاتيح مشفّر لكل مستخدم** | 2026-08-20 |
| ✅ | **المهمة ٤ — صفحة `/settings` للحساب** | 2026-08-20 |

---

# مخاطر مفتوحة

| # | الخطر | الحالة |
|---|---|---|
| ~~١~~ | ~~لا عزل بيانات~~ | ✅ أُغلق |
| ~~٢~~ | ~~`.env.local` ملف واحد~~ | ✅ أُغلق |
| ٨ | **فقدان `CREDENTIALS_KEY` يجعل كل المفاتيح المخزّنة غير قابلة للقراءة** — احفظ نسخة منه | تشغيلي |
| ٣ | لا حد لمحاولات الدخول ولا 2FA ولا استرجاع | المهمة ٥ |
| ٤ | SQLite بكاتب واحد | المهمة ٦ |
| ٥ | 13 ثغرة في الحزم (واحدة حرجة) | المهمة ٦ |
| ٦ | المجدول داخل العملية — يتوقف بتوقف الخادم | المهمة ٨ |
| ٧ | بقايا محتوى المالك السابق في البذر (`wf-vantage-sales`، ١٤ اسماً وهمياً) | مع إعادة كتابة البذر |
